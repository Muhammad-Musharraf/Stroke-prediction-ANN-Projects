[project]
name = "stroke-prediction"
version = "0.1.0"
description = "Stroke Prediction API"
requires-python = ">=3.12"

dependencies = [
    "fastapi[standard]",
    "uvicorn",
    "scikit-learn==1.6.1",
    "pandas",
    "numpy",
    "joblib",
    "tensorflow==2.20.0",
    "sentry-sdk>=2.66.1",
    "python-dotenv",
    "streamlit>=1.60.0",
    "requests",
    "plotly>=6.9.0",
]
