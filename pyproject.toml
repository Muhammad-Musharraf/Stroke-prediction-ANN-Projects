[project]
name = "stroke-prediction"
version = "0.1.0"
description = "Stroke Prediction API"
requires-python = ">=3.12"

dependencies = [
    "fastapi",
    "uvicorn",
    "scikit-learn",
    "pandas",
    "numpy",
    "joblib",
    "tensorflow==2.20.0",
]                                                                                                                                                                                                                                                                                                                                      
