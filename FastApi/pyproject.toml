[project]
name = "my-app"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "fastapi[standard]",
    "uvicorn[standard]",
]

[tool.fastapi-cloud]
start-command = "uvicorn main:app --host 0.0.0.0 --port 8000"
   
